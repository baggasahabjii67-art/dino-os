const mangaLibrary = [
  { id: 'solo-leveling', title: 'Solo Leveling', author: 'Chugong', chapters: 179, source: 'MangaDex', cover: 'https://images.unsplash.com/photo-1578632767115-351597cf2477?w=600&q=80' },
  { id: 'one-piece', title: 'One Piece', author: 'Eiichiro Oda', chapters: 1120, source: 'MangaDex', cover: 'https://images.unsplash.com/photo-1613376023733-0a73315d9b0d?w=600&q=80' },
  { id: 'chainsaw-man', title: 'Chainsaw Man', author: 'Tatsuki Fujimoto', chapters: 172, source: 'AniList', cover: 'https://images.unsplash.com/photo-1607604276583-eef5d076a3f5?w=600&q=80' },
  { id: 'frieren', title: 'Frieren: Beyond Journey’s End', author: 'Kanehito Yamada', chapters: 140, source: 'AniList', cover: 'https://images.unsplash.com/photo-1535016120720-40c646be5580?w=600&q=80' }
];
const sourceList = [
  { id: 'mangadex', name: 'MangaDex', language: 'All languages', installed: true },
  { id: 'anilist', name: 'AniList', language: 'All languages', installed: true },
  { id: 'mangafreak', name: 'MangaFreak', language: 'English', installed: false },
  { id: 'mangakakalot', name: 'MangaKakalot', language: 'English', installed: false },
  { id: 'mangasee', name: 'MangaSee', language: 'English', installed: false }
];
const APP_VERSION = '004';
const defaults = { theme: 'Dark', layout: 'Comfortable', accent: 'Purple', fontSize: 'Medium', readingDirection: 'Vertical', pageFit: 'Width', resolution: 'Auto', optimization: 'Balanced', preload: 'Enabled', animations: 'Enabled', incognito: 'Disabled' };
const options = { theme: ['Dark', 'Light', 'Midnight'], layout: ['Compact', 'Comfortable', 'Spacious'], accent: ['Purple', 'Blue', 'Green', 'Orange'], fontSize: ['Small', 'Medium', 'Large'], readingDirection: ['Vertical', 'Horizontal'], pageFit: ['Width', 'Height', 'Original'], resolution: ['Auto', '720p', '1080p', '1440p'], optimization: ['Battery saver', 'Balanced', 'Quality'], preload: ['Enabled', 'Disabled'], animations: ['Enabled', 'Disabled'], incognito: ['Enabled', 'Disabled'] };
const labels = { theme: 'Theme', layout: 'Layout density', accent: 'Accent color', fontSize: 'Reader font size', readingDirection: 'Reading direction', pageFit: 'Page fit', resolution: 'Resolution', optimization: 'Optimization mode', preload: 'Preload pages', animations: 'Animations', incognito: 'Incognito mode' };
const app = document.getElementById('app');
let settings = loadObject('dino-reader-settings', defaults);
let extensionRepos = loadArray('dino-extension-repos');
let extensions = loadArray('dino-extensions');
let feedback = loadArray('dino-feedback');
let currentTab = 'manga'; let searchText = '';
function loadObject(key, fallback) { try { return { ...fallback, ...JSON.parse(localStorage.getItem(key) || '{}') }; } catch { return { ...fallback }; } }
function loadArray(key) { try { const value = JSON.parse(localStorage.getItem(key) || '[]'); return Array.isArray(value) ? value : []; } catch { return []; } }
function save(key, value) { localStorage.setItem(key, JSON.stringify(value)); }
function escapeHtml(value) { return String(value).replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c])); }
function navButton(id, icon, label) { return `<button class="nav ${currentTab === id ? 'active' : ''}" data-nav="${id}"><span>${icon}</span><label>${label}</label></button>`; }
function applySettings() { const root = document.documentElement; const themes = { Dark: ['#101216', '#181b21', '#222731', '#f1f3f5'], Light: ['#f3f5f8', '#fff', '#e5e9f0', '#18202b'], Midnight: ['#070b18', '#10172b', '#192442', '#eef3ff'] }; const accents = { Purple: '#a78bfa', Blue: '#60a5fa', Green: '#69d391', Orange: '#fb923c' }; const [bg, panel, panel2, text] = themes[settings.theme] || themes.Dark; root.style.setProperty('--bg', bg); root.style.setProperty('--panel', panel); root.style.setProperty('--panel2', panel2); root.style.setProperty('--text', text); root.style.setProperty('--brand', accents[settings.accent] || accents.Purple); root.style.setProperty('--base-size', { Small: '14px', Medium: '16px', Large: '18px' }[settings.fontSize]); root.style.setProperty('--card-padding', { Compact: '10px', Comfortable: '14px', Spacious: '20px' }[settings.layout]); document.body.dataset.animations = settings.animations.toLowerCase(); document.body.dataset.incognito = settings.incognito.toLowerCase(); }
function renderCard(m) { return `<article class="card"><div class="cover"><img src="${m.cover}" alt="${escapeHtml(m.title)}"><span class="badge">${escapeHtml(m.source)}</span></div><h3>${escapeHtml(m.title)}</h3><p>${escapeHtml(m.author)} · ${m.chapters} chapters</p><button class="btn small" data-read="${m.id}">Read</button></article>`; }
function renderManga() { const list = mangaLibrary.filter((m) => `${m.title} ${m.author}`.toLowerCase().includes(searchText.toLowerCase())); return `<section><div class="heading"><div><h1>Manga</h1><p class="muted">Your manga library</p></div></div><div class="grid">${list.map(renderCard).join('') || '<div class="empty">No manga matches your search.</div>'}</div></section>`; }
function renderHistory() { return `<section><h1>History</h1><p class="muted">${settings.incognito === 'Enabled' ? 'Incognito mode is enabled. Reading history is not saved.' : 'No recent chapters yet.'}</p></section>`; }
function renderSources() { return `<section><h1>Sources</h1><p class="muted">Built-in and extension sources</p><div class="panel">${sourceList.concat(extensions).map((s) => `<div class="row"><div><b>${escapeHtml(s.name)}</b><p class="muted">${escapeHtml(s.language || 'Extension source')}</p></div><span class="badge-static">${s.installed === false ? 'Available' : 'Installed'}</span></div>`).join('')}</div></section>`; }
function settingControl(key) { return `<label class="setting"><span>${labels[key]}</span><select data-setting="${key}">${options[key].map((v) => `<option ${settings[key] === v ? 'selected' : ''}>${v}</option>`).join('')}</select></label>`; }
function renderSettings() { return `<section><h1>Settings & Customization</h1><p class="muted">Changes apply immediately and are saved automatically.</p><div class="settings-grid">${Object.keys(options).map(settingControl).join('')}</div><div class="settings-actions"><button class="btn" data-reset>Reset to defaults</button></div></section>`; }
function renderDetails() { return `<section><h1>Version & Details</h1><div class="panel details"><div class="detail-row"><b>Version</b><strong>${APP_VERSION}</strong></div><div class="detail-row"><b>Application</b><span>DinoReaders Manga Reader</span></div><div class="detail-row"><b>Features</b><span>Library, reader preview, sources, customization, extension repositories and feedback</span></div></div></section>`; }
function renderRepo() { return `<section><h1>Extension Repository</h1><p class="muted">Paste a public GitHub repository containing a JSON extension manifest.</p><form class="repo-form" id="repoForm"><input id="repoUrl" class="search" type="url" placeholder="https://github.com/owner/repository" required><button class="btn" type="submit">Add repository</button></form><div class="panel repo-list">${extensionRepos.length ? extensionRepos.map((r, i) => `<div class="row"><div><b>${escapeHtml(r)}</b><p class="muted">Saved extension repository</p></div><button class="btn small" data-remove-repo="${i}">Remove</button></div>`).join('') : '<p class="muted">No extension repositories added.</p>'}</div><p class="muted repo-status" id="repoStatus"></p></section>`; }
function renderFeedback() { return `<section><h1>Feedback</h1><p class="muted">Tell us what you like or what should be improved.</p><form class="feedback-form" id="feedbackForm"><label>Your name <input id="feedbackName" maxlength="60" placeholder="Optional"></label><label>Type <select id="feedbackType"><option>Suggestion</option><option>Bug report</option><option>Feature request</option><option>Other</option></select></label><label>Message <textarea id="feedbackMessage" maxlength="1000" rows="6" placeholder="Write your feedback..." required></textarea></label><button class="btn" type="submit">Save feedback</button></form><div class="panel feedback-list">${feedback.length ? feedback.map((item) => `<div class="feedback-item"><b>${escapeHtml(item.type)}</b><p>${escapeHtml(item.message)}</p><small class="muted">${escapeHtml(item.name || 'Anonymous')} · ${new Date(item.createdAt).toLocaleString()}</small></div>`).join('') : '<p class="muted">No feedback submitted yet.</p>'}</div><p class="muted" id="feedbackStatus"></p></section>`; }
async function fetchRepo(url) { const match = url.match(/^https?:\/\/github\.com\/([^/]+)\/([^/#?]+)\/?$/); if (!match) throw new Error('Use a public GitHub repository URL.'); const [, owner, repo] = match; for (const file of ['extensions.json', 'sources.json', 'repo.json']) { const response = await fetch(`https://raw.githubusercontent.com/${owner}/${repo}/main/${file}`); if (!response.ok) continue; const data = await response.json(); const list = Array.isArray(data) ? data : (data.extensions || data.sources || []); return list.map((item) => typeof item === 'string' ? { name: item, language: 'Extension source', installed: true } : { name: item.name || item.title || 'Unnamed extension', language: item.language || 'Extension source', installed: true }); } throw new Error('No extensions.json, sources.json, or repo.json found on the main branch.'); }
function openReader(m) { const modal = document.createElement('div'); modal.className = 'reader-modal'; modal.innerHTML = `<div class="reader-box"><button class="close" aria-label="Close">×</button><h1>${escapeHtml(m.title)}</h1><div class="reader-page"><img src="${m.cover}" alt="${escapeHtml(m.title)}"><p>Chapter content is not available from this source yet.</p></div></div>`; document.body.append(modal); const close = () => modal.remove(); modal.querySelector('.close').onclick = close; modal.onclick = (e) => { if (e.target === modal) close(); }; }
function render() { if (!app) return; applySettings(); const page = currentTab === 'manga' ? renderManga() : currentTab === 'history' ? renderHistory() : currentTab === 'sources' ? renderSources() : currentTab === 'settings' ? renderSettings() : currentTab === 'details' ? renderDetails() : currentTab === 'repo' ? renderRepo() : renderFeedback(); app.innerHTML = `<div class="shell"><aside><div class="brand">Dino<span>Readers</span></div>${navButton('manga', '▦', 'Manga')}${navButton('history', '◷', 'History')}${navButton('sources', '◉', 'Sources')}${navButton('settings', '⚙', 'Settings')}<div class="sidebar-version">Version ${APP_VERSION}</div>${navButton('details', 'ⓘ', 'Details')}${navButton('repo', '⌘', 'Extension Repo')}${navButton('feedback', '✉', 'Feedback')}</aside><main><header><input id="searchInput" class="search" placeholder="Search manga..." value="${escapeHtml(searchText)}"></header>${page}</main></div>`;
  document.querySelectorAll('[data-nav]').forEach((b) => b.onclick = () => { currentTab = b.dataset.nav; render(); }); const search = document.getElementById('searchInput'); if (search) search.oninput = (e) => { searchText = e.target.value; render(); }; document.querySelectorAll('[data-read]').forEach((b) => b.onclick = () => { const m = mangaLibrary.find((x) => x.id === b.dataset.read); if (m) openReader(m); }); document.querySelectorAll('[data-setting]').forEach((c) => c.onchange = () => { settings[c.dataset.setting] = c.value; save('dino-reader-settings', settings); render(); }); const reset = document.querySelector('[data-reset]'); if (reset) reset.onclick = () => { settings = { ...defaults }; save('dino-reader-settings', settings); render(); }; document.querySelectorAll('[data-remove-repo]').forEach((b) => b.onclick = () => { extensionRepos.splice(Number(b.dataset.removeRepo), 1); save('dino-extension-repos', extensionRepos); render(); }); const repoForm = document.getElementById('repoForm'); if (repoForm) repoForm.onsubmit = async (e) => { e.preventDefault(); const input = document.getElementById('repoUrl'); const status = document.getElementById('repoStatus'); const url = input.value.trim().replace(/\/$/, ''); status.textContent = 'Loading extensions…'; try { const found = await fetchRepo(url); extensionRepos = [...new Set([...extensionRepos, url])]; extensions = [...extensions, ...found.filter((x) => !extensions.some((item) => item.name === x.name))]; save('dino-extension-repos', extensionRepos); save('dino-extensions', extensions); status.textContent = `Loaded ${found.length} extension${found.length === 1 ? '' : 's'}.`; render(); } catch (error) { status.textContent = error.message; } }; const feedbackForm = document.getElementById('feedbackForm'); if (feedbackForm) feedbackForm.onsubmit = (e) => { e.preventDefault(); const message = document.getElementById('feedbackMessage').value.trim(); if (!message) return; feedback.unshift({ name: document.getElementById('feedbackName').value.trim(), type: document.getElementById('feedbackType').value, message, createdAt: new Date().toISOString() }); feedback = feedback.slice(0, 30); save('dino-feedback', feedback); render(); }; }
if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', render); else render();
